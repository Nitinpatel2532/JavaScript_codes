//1.) Arrow function

// Arrow function is a shortway to write regular functions.if we write to function in a single line then
// we use arrow function....It uses the => arrow syntax,and doesnt require function keyword....

// If the function has only one line and return something you can skip and return the keyword..

const mul = (a, b) => { return a * b }
document.write(5, 6)
